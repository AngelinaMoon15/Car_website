# 🚗 Car Rental Landing Page

![Status](https://img.shields.io/badge/Статус-Завершён-brightgreen)
![Type](https://img.shields.io/badge/Тип-Учебный%20проект-blue)

---

## 📄 О проекте (About)

Этот репозиторий содержит лендинг сервиса аренды автомобилей. Проект выполнен в рамках **учебной практики** по фронтенд-разработке.

🎯 **Задача:** Сверстать многосекционный лендинг по макету с нуля, реализовать интерактивную фильтрацию каталога и форму заявки с валидацией. Важным требованием была корректная работа на всех устройствах и чистота кода без использования сторонних библиотек.

---

## 🛠️ Технологический стек (Tech Stack)

- **HTML5** (Семантическая вёрстка)
- **CSS3**
  - `Flexbox` — для построения лейаутов и выравнивания элементов
  - `position: sticky` — для «липкого» сайдбара фильтра
  - `Media Queries` — для реализации адаптивности
- **JavaScript ES6+**
  - DOM-манипуляции
  - Обработка событий
  - Валидация форм с регулярными выражениями

---

## ⚡ Ключевые особенности (Key Features)

### 1. Интерактивная фильтрация каталога (DOM Filter)

Пользователь может фильтровать автомобили по марке без перезагрузки страницы.

- **Решение:** При клике на элемент фильтра скрипт сравнивает `textContent` кнопки с заголовком `<h4>` каждой карточки и переключает `display` через `car.style.display`.
- **Результат:** Мгновенная фильтрация без запросов к серверу; активный пункт подсвечивается через класс `.active`.

### 2. Валидация формы с живой обратной связью (Form Validation)

Форма заявки проверяет поля до отправки и подсвечивает ошибки в реальном времени.

- **Решение:** Для каждого поля написана отдельная функция-валидатор. Телефон проверяется через `replace(/[\s\-\+\(\)]/g, '')` + проверку длины. Имя — через regex `/^[a-zA-Zа-яА-ЯёЁ\s]+$/`.
- **Результат:** Красная рамка появляется сразу при вводе (`input`) и потере фокуса (`blur`). Форма отправляется только если все поля прошли валидацию.

### 3. Полная адаптивность (Responsive Design)

Вёрстка корректно отображается на четырёх ключевых брейкпоинтах:

- 🖥️ **Desktop (1200px+):** Полноценный двухколоночный лейаут с абсолютно спозиционированными декоративными элементами.
- 📟 **Tablet (≤1024px):** Переход фильтра в горизонтальный ряд, карточки перестраиваются в одну колонку.
- 📱 **Mobile (≤767px):** Хедер складывается вертикально, декоративный чёрный блок скрывается (`display: none`), кнопки растягиваются на всю ширину.
- 🤏 **Small (≤480px):** Уменьшение шрифтов заголовков, упрощение отступов.

### 4. Sticky-сайдбар фильтра (Sticky Sidebar)

- **Решение:** `position: sticky` + `top: 20px` на блоке `.cars-filter`.
- **Результат:** Фильтр остаётся в зоне видимости при скролле по длинному каталогу, не мешая контенту.

---

## 🚀 Как запустить (How to run)

Так как это статический проект, достаточно открыть файл `index.html` в любом современном браузере.

```bash
# Или через Live Server в VS Code:
# ПКМ на index.html → Open with Live Server
